{
  "puppet_task_version": 1,
  "supports_noop": false,
  "description": "Allows users to run apachectl through puppet",
  "parameters": {
     "command": {
        "description": "Which apachectl to run",
	"type": "Enum[start,stop,restart,status,graceful,graceful-stop]"
     }
  }
}
